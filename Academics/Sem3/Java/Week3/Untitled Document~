//Fibonacci Series
import java.util.Scanner;
class FibSeries{
static int fib(int x)
{
 if(x==0)
 return(0);
 if(x==1)
 return(1);
 return(fib(x-1)+fib(x-2));
}
public static void main(String[] args)
{
Scanner s=new Scanner(System.in);
System.out.println("Enter the number for which Fibonacci Series is to be generated:");
int n=s.nextInt();
for(int i=0;i<n; i++)
System.out.println(fib(i));
}
} 
